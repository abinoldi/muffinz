#!/bin/bash

# ==========================================================
# ROBLOX CLONE MANAGER v5.0 (PURE BASH)
# ==========================================================

# --- KONFIGURASI MANAGER ---
BACKUP_DIR="/sdcard/RobloxBackups"
DOWNLOAD_DIR="/sdcard/Download/RobloxAPKs"
GOFILE_URL="https://gofile.io/d/ewZLec"  # Ganti dengan link kamu
TOKEN="" # Biarkan kosong untuk guest

# --- WARNA OUTPUT ---
GREEN='\033[0;32m'
RED='\033[0;31m'
YELLOW='\033[1;33m'
NC='\033[0m'

_print() { echo -e "$1"; }

# ==========================================================
# BAGIAN 1: GOFILE DOWNLOADER ENGINE (BASH)
# ==========================================================

parse_json() {
    local key=$1
    local json=$2
    echo "$json" | sed -n 's/.*"'"$key"'":"\([^"]*\)".*/\1/p' | head -n 1
}

parse_children_ids() {
    # Menggunakan grep -oP butuh grep versi penuh (biasanya ada di Termux)
    echo "$1" | grep -oP '"id":"\K[^"]+' | tail -n +2
}

setup_token() {
    if [ -z "$TOKEN" ]; then
        _print "${YELLOW}🔑 Meminta token akses tamu...${NC}"
        local response=$(curl -s -X POST https://api.gofile.io/accounts)
        TOKEN=$(parse_json "token" "$response")
        if [ -z "$TOKEN" ] || [ "$TOKEN" == "null" ]; then 
            _print "${RED}❌ Gagal mendapatkan token.${NC}"
            exit 1
        fi
    fi
}

download_file() {
    local url="$1"
    local output_path="$2"
    local filename=$(basename "$output_path")
    
    if [ -f "$output_path" ]; then
        _print "⏩ Skip (Sudah ada): $filename"
        return
    fi

    _print "⬇️ Download: ${GREEN}$filename${NC}"
    
    # Retry 3 kali, progress bar sederhana
    curl -L -# --retry 3 \
         -H "Authorization: Bearer $TOKEN" \
         -o "$output_path" \
         "$url"
}

fetch_contents() {
    local content_id="$1"
    local current_dir="$2"
    local password="$3"

    # API Request
    local api_url="https://api.gofile.io/contents/$content_id?cache=true"
    [ -n "$password" ] && api_url+="&password=$(echo -n $password | sha256sum | awk '{print $1}')"

    local response=$(curl -s -H "X-Website-Token: 4fd6sg89d7s6" -H "Authorization: Bearer $TOKEN" "$api_url")
    
    local status=$(parse_json "status" "$response")
    if [ "$status" != "ok" ]; then
        _print "${RED}❌ Gagal akses ID: $content_id${NC}"
        return
    fi

    local type=$(parse_json "type" "$response")
    local name=$(parse_json "name" "$response")
    
    # Jika root folder dari link, jangan buat subfolder lagi agar rapi
    if [[ "$current_dir" == "$DOWNLOAD_DIR" ]] && [[ "$type" == "folder" ]]; then
         local target_path="$current_dir"
    else
         local target_path="$current_dir/$name"
    fi

    if [ "$type" == "folder" ]; then
        mkdir -p "$target_path"
        _print "${YELLOW}📂 Memindai Folder: $name${NC}"
        
        # Parsing ID anak (child ids)
        local ids=$(echo "$response" | grep -oP '"id":"\K[^"]+' | tail -n +2)
        
        # Loop download (Sequential agar tidak crash di HP kentang)
        for id in $ids; do
            fetch_contents "$id" "$target_path" "$password"
        done
    else
        local link=$(parse_json "link" "$response")
        download_file "$link" "$target_path"
    fi
}

# ==========================================================
# BAGIAN 2: ROBLOX MANAGER UTILS
# ==========================================================

check_root() {
    if [ "$(id -u)" -ne 0 ]; then
        _print "${RED}❌ Script ini MEMBUTUHKAN akses Root!${NC}"
        _print "👉 Jalankan dengan: ${YELLOW}tsu${NC} atau ${YELLOW}sudo bash namafile.sh${NC}"
        exit 1
    fi
}

do_backup() {
    _print "\n=== MEMULAI BACKUP ==="
    mkdir -p "$BACKUP_DIR"
    count=0
    for folder in /data/data/com.roblox.clien*; do
        [ -e "$folder" ] || continue
        pkg_name=$(basename "$folder")
        cookie_path=""
        
        # Cek berbagai kemungkinan lokasi cookies
        if [ -f "$folder/app_webview/Default/Cookies" ]; then
            cookie_path="$folder/app_webview/Default/Cookies"
        elif [ -f "$folder/app_webview/Cookies" ]; then
            cookie_path="$folder/app_webview/Cookies"
        fi

        if [ -n "$cookie_path" ]; then
            cp "$cookie_path" "$BACKUP_DIR/${pkg_name}.db"
            _print "✅ Backup: $pkg_name"
            count=$((count + 1))
        fi
    done
    _print "Selesai! $count database tersimpan."
}

do_restore_sequential() {
    _print "\n=== RESTORE SEQUENTIAL ==="
    
    # Ambil list backup dan aplikasi
    backups=($(ls "$BACKUP_DIR"/com.roblox.clien*.db 2>/dev/null))
    pkgs=($(pm list packages | grep "com.roblox.clien" | cut -f 2 -d ":"))

    total_backups=${#backups[@]}
    total_pkgs=${#pkgs[@]}

    if [ "$total_backups" -eq 0 ] || [ "$total_pkgs" -eq 0 ]; then
        _print "${RED}❌ Backup atau Aplikasi tidak ditemukan.${NC}"
        return
    fi

    i=0
    while [ $i -lt $total_backups ] && [ $i -lt $total_pkgs ]; do
        current_db="${backups[$i]}"
        current_pkg="${pkgs[$i]}"
        
        target_folder="/data/data/$current_pkg"
        target_dir_parent="$target_folder/app_webview/Default"
        target_path="$target_dir_parent/Cookies"

        _print "🔄 Restore [$(basename "$current_db")] -> [$current_pkg]"

        mkdir -p "$target_dir_parent"
        cp "$current_db" "$target_path"
        
        # Perbaikan Izin Akses (PENTING)
        app_uid=$(stat -c "%u:%g" "$target_folder")
        if [ -n "$app_uid" ]; then
            chown -R "$app_uid" "$target_folder/app_webview"
            chmod 660 "$target_path"
        fi
        
        i=$((i + 1))
    done
    _print "✅ Selesai! $i akun dipulihkan."
}

do_uninstall() {
    _print "\n=== HAPUS APLIKASI CLONE ==="
    pkgs=$(pm list packages | grep "com.roblox.clien" | cut -f 2 -d ":")
    
    if [ -z "$pkgs" ]; then
        _print "${RED}❌ Tidak ada aplikasi terinstall.${NC}"
        return
    fi
    
    echo -n "❓ Yakin hapus SEMUA? (y/n): "
    read confirm
    if [[ "$confirm" == "y" || "$confirm" == "Y" ]]; then
        for pkg in $pkgs; do
            pm uninstall "$pkg" > /dev/null 2>&1
            _print "🗑️ $pkg Terhapus."
        done
    fi
}

do_auto_process() {
    _print "\n=========================================="
    _print "   PROSES OTOMATIS (BASH NATIVE)          "
    _print "=========================================="
    
    mkdir -p "$DOWNLOAD_DIR"

    # 1. Jalankan Downloader
    local content_id=$(echo "$GOFILE_URL" | grep -oE '[a-zA-Z0-9]+$' | tail -n 1)
    
    setup_token
    _print "🚀 Memulai download dari Gofile ID: $content_id"
    fetch_contents "$content_id" "$DOWNLOAD_DIR" ""
    
    _print "\n✅ Download Selesai. Memulai Instalasi..."
    
    # 2. Install APK (Looping semua .apk di folder download)
    count_install=0
    
    # Atasi spasi pada nama file dengan while read
    find "$DOWNLOAD_DIR" -maxdepth 2 -name "*.apk" | while read apk_file; do
        _print "⏳ Memasang: $(basename "$apk_file")..."
        pm install -r "$apk_file" > /dev/null 2>&1
        # Kita tidak bisa increment variabel di dalam pipe subshell dengan mudah di sh,
        # jadi kita biarkan log saja.
    done

    # 3. Restore
    do_restore_sequential
    _print "✨ SEMUA PROSES SELESAI!"
}


# ==========================================================
# MAIN LOOP
# ==========================================================
check_root

while true; do
    echo ""
    _print "${GREEN}=== ROBLOX MANAGER v5.0 (BASH) ===${NC}"
    echo "1. Backup Akun"
    echo "2. Restore Akun (Urutan Sequential)"
    echo "3. Hapus Semua Aplikasi Clone"
    echo "4. PROSES OTOMATIS (Download -> Install -> Restore)"
    echo "5. Keluar"
    echo "------------------------------------------"
    echo -n "Pilih menu [1-5]: "
    read choice
  
    case $choice in
        1) do_backup ;;
        2) do_restore_sequential ;;
        3) do_uninstall ;;
        4) do_auto_process ;;
        5) exit 0 ;;
        *) echo "⚠️ Pilihan salah." ;;
    esac
done
