#!/system/bin/sh

# ==========================================================
# ROBLOX CLONE MANAGER v3.5 (STABLE - 1 BY 1 DOWNLOAD)
# ==========================================================

BACKUP_DIR="/sdcard/RobloxBackups"
DOWNLOAD_DIR="/sdcard/Download/RobloxAPKs"
BASE_URL="http://43.134.116.2:5555/uploaded"

# KONFIGURASI TIMEOUT (PENTING AGAR TIDAK STUCK)
TIMEOUT_SEC=300      # 5 Menit per file. Kalau macet, skip.

check_root() {
    if [ "$(id -u)" -ne 0 ]; then
        echo "❌ Script ini MEMBUTUHKAN akses Root!"
        exit 1
    fi
}

do_backup() {
    echo "=========================================="
    echo "       MEMULAI PROSES BACKUP...           "
    echo "=========================================="
    mkdir -p "$BACKUP_DIR"
    count=0
    for folder in /data/data/com.roblox.clien*; do
        [ -e "$folder" ] || continue
        pkg_name=$(basename "$folder")
        
        cookie_file=""
        [ -f "$folder/app_webview/Default/Cookies" ] && cookie_file="$folder/app_webview/Default/Cookies"
        [ -f "$folder/app_webview/Cookies" ] && cookie_file="$folder/app_webview/Cookies"

        if [ -n "$cookie_file" ]; then
             cp "$cookie_file" "$BACKUP_DIR/${pkg_name}.db"
             count=$((count + 1))
        fi
    done
    echo "✅ Backup Selesai! $count akun tersimpan."
}

do_restore_sequential() {
    echo ""
    echo "=========================================="
    echo "    RESTORE BERDASARKAN URUTAN (SEQ)      "
    echo "=========================================="
    backups=($(ls "$BACKUP_DIR"/com.roblox.clien*.db 2>/dev/null))
    pkgs=($(pm list packages | grep "com.roblox.clien" | cut -f 2 -d ":"))

    total_backups=${#backups[@]}
    if [ "$total_backups" -eq 0 ]; then echo "❌ Tidak ada backup."; return 1; fi

    i=0
    while [ $i -lt $total_backups ] && [ $i -lt ${#pkgs[@]} ]; do
        current_db="${backups[$i]}"
        current_pkg="${pkgs[$i]}"
        target_dir="/data/data/$current_pkg/app_webview/Default"
        
        echo "🔄 Restore: $current_pkg"
        mkdir -p "$target_dir"
        cp "$current_db" "$target_dir/Cookies"
        
        uid=$(stat -c "%u" "/data/data/$current_pkg")
        chown -R "$uid:$uid" "/data/data/$current_pkg/app_webview"
        chmod 660 "$target_dir/Cookies"
        
        i=$((i + 1))
    done
    echo "✅ Restore Selesai."
}

do_uninstall() {
    echo "🗑️ Menghapus aplikasi..."
    for pkg in $(pm list packages | grep "com.roblox.clien" | cut -f 2 -d ":"); do
        pm uninstall "$pkg" > /dev/null 2>&1
    done
    echo "✅ Bersih."
}

do_auto_process() {
    echo "=========================================="
    echo "   🚀 PROSES OTOMATIS (1 BY 1 MODE) 🚀    "
    echo "=========================================="
    
    total_files=$(ls "$BACKUP_DIR"/com.roblox.clien*.db 2>/dev/null | wc -l)
    if [ "$total_files" -eq 0 ]; then echo "❌ Tidak ada backup."; return; fi

    # Bersihkan folder download
    rm -rf "$DOWNLOAD_DIR"
    mkdir -p "$DOWNLOAD_DIR"

    echo "📡 Total file yang akan diproses: $total_files"
    echo "⚠️  Mode: Sequential (Satu per satu agar aman)"
    echo "------------------------------------------"

    # --- LOOP DOWNLOAD 1 PER 1 ---
    for i in $(seq 1 "$total_files"); do
        APK_NAME="$i.apk"
        SAVE_PATH="$DOWNLOAD_DIR/$APK_NAME"
        
        echo -n "⬇️  [$i/$total_files] Mendownload $APK_NAME... "
        
        # Penjelasan Command CURL:
        # -L : Follow redirect
        # -f : Fail silent (kalau 404 gak bikin file kosong)
        # --retry 3 : Coba 3x kalau gagal
        # -m 300 : Max time 5 menit
        # -# : Tampilkan progress bar sederhana (Hashtag)
        
        curl -L -f --retry 3 --retry-delay 2 -m $TIMEOUT_SEC "$BASE_URL/$APK_NAME" -o "$SAVE_PATH" 
        
        # Cek status exit code curl ($?)
        if [ $? -eq 0 ]; then
            echo " ✅ Sukses."
        else
            echo " ❌ GAGAL! (Server timeout/File tidak ada)"
            # Kita lanjut ke file berikutnya, jangan exit script
        fi
    done

    echo "------------------------------------------"
    echo "📦 [PHASE 2] Menginstall Aplikasi..."
    
    installed_count=0
    for i in $(seq 1 "$total_files"); do
        APK_NAME="$i.apk"
        SAVE_PATH="$DOWNLOAD_DIR/$APK_NAME"
        
        if [ -f "$SAVE_PATH" ] && [ $(stat -c%s "$SAVE_PATH") -gt 1000 ]; then
            echo "   💿 Menginstall $APK_NAME..."
            pm install -r "$SAVE_PATH" > /dev/null 2>&1
            installed_count=$((installed_count + 1))
        else
            echo "   ⚠️ File $APK_NAME rusak/tidak terdownload."
        fi
    done

    do_restore_sequential
    echo "✨ SELESAI! ($installed_count berhasil diinstall)"
}

# --- MAIN MENU ---
check_root
while true; do
    echo ""
    echo "=========================================="
    echo "    ROBLOX MANAGER v3.5 (SAFE MODE)       "
    echo "=========================================="
    echo "1. Backup Akun"
    echo "2. Restore Akun"
    echo "3. Hapus Semua Aplikasi"
    echo "4. MULAI PROSES (Download 1-by-1)"
    echo "5. Keluar"
    echo -n "Pilih: "
    read choice
    case $choice in
        1) do_backup ;;
        2) do_restore_sequential ;;
        3) do_uninstall ;;
        4) do_auto_process ;;
        5) exit 0 ;;
        *) echo "⚠️ Salah." ;;
    esac
done
