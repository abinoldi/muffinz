repeat task.wait() until game:IsLoaded()

-- ======================================================
-- # SYNCEHUB x RE-FARM ULTIMATE V8.6 (MERGED HYBRID)
-- LVCB (Progression) -> Remfarm (Endgame Farming)
-- UPDATE: GLOBAL POTATO MODE, GUI Counter, Auto Sell, Auto Trade Evolved Stone, Recheck Lever Fix
-- ======================================================

_G.EnableGlobalPotatoMode = true -- Set ke false jika tidak ingin grafik burik

local Services = {
    Players = game:GetService("Players"),
    ReplicatedStorage = game:GetService("ReplicatedStorage"),
    RunService = game:GetService("RunService"),
    HttpService = game:GetService("HttpService"),
    CoreGui = game:GetService("CoreGui"),
    Lighting = game:GetService("Lighting"),
    UserInputService = game:GetService("UserInputService")
}

local lp = Services.Players.LocalPlayer
local req = request or http_request or syn.request

print("\n=============================================")
print("🚀 [Ultimate All-in-One] LVCB -> Remfarm Dimulai")
print("=============================================\n")

-- ======================================================
-- 1. SHARED SERVICES & REMOTE CACHING
-- ======================================================
local Replion, ItemUtility, TierUtility, PlayerData, MerchantReplion
pcall(function()
    Replion = require(Services.ReplicatedStorage:WaitForChild("Packages", 5):WaitForChild("Replion", 5)).Client
    ItemUtility = require(Services.ReplicatedStorage:WaitForChild("Shared", 5):WaitForChild("ItemUtility", 5))
    TierUtility = require(Services.ReplicatedStorage:WaitForChild("Shared", 5):WaitForChild("TierUtility", 5))
    PlayerData = Replion:WaitReplion("Data", 10)
    MerchantReplion = Replion:WaitReplion("Merchant", 10)
end)

local CachedRemotes = {}
local TargetRemotes = {
    "RF/ChargeFishingRod", "RF/RequestFishingMinigameStarted", "RF/CatchFishCompleted", "RE/FishingCompleted", 
    "RF/EquipToolFromHotbar", "RE/EquipToolFromHotbar", "RF/SellAllItems", "RE/FavoriteItem", 
    "RE/PlaceLeverItem", "RE/PlacePressureItem", "RE/SpawnTotem", "RF/EquipOxygenTank", 
    "RF/UnequipOxygenTank", "RF/InitiateTrade", "RF/PurchaseMarketItem", "RF/UpdateAutoSellThreshold", 
    "RE/ObtainedNewFishNotification", "RF/PurchaseWeatherEvent"
}

local function InitializeRemotes()
    local netFolder = Services.ReplicatedStorage:FindFirstChild("Packages")
    if netFolder then
        netFolder = netFolder:FindFirstChild("_Index")
        if netFolder then
            netFolder = netFolder:FindFirstChild("sleitnick_net@0.2.0")
            if netFolder then
                netFolder = netFolder:FindFirstChild("net")
                if netFolder then
                    local children = netFolder:GetChildren()
                    local targetMap = {}
                    for _, name in ipairs(TargetRemotes) do targetMap[name] = true end
                    for i = 1, #children do
                        if targetMap[children[i].Name] and i < #children then
                            CachedRemotes[children[i].Name] = children[i + 1]
                        end
                    end
                end
            end
        end
    end
end

InitializeRemotes()

local function GetRemote(name) return CachedRemotes[name] end

local function FireRemote(remote, ...)
    if not remote then return false end
    local args = {...}
    if #args == 0 then args = {1} end
    task.spawn(function()
        pcall(function()
            if remote:IsA("RemoteFunction") then remote:InvokeServer(unpack(args)) else remote:FireServer(unpack(args)) end
        end)
    end)
    return true
end

local RF_ChargeFishingRod = GetRemote("RF/ChargeFishingRod")
local RF_RequestFishingMinigameStarted = GetRemote("RF/RequestFishingMinigameStarted")
local RE_FishingCompleted = GetRemote("RF/CatchFishCompleted") or GetRemote("RE/FishingCompleted")
local Remote_EquipTool = GetRemote("RF/EquipToolFromHotbar") or GetRemote("RE/EquipToolFromHotbar")
local RE_FavoriteItem = GetRemote("RE/FavoriteItem")
local RE_PlaceLeverItem = GetRemote("RE/PlaceLeverItem")
local RE_PlacePressureItem = GetRemote("RE/PlacePressureItem")
local RF_SellAllItems = GetRemote("RF/SellAllItems")
local RE_ObtainedNewFishNotification = GetRemote("RE/ObtainedNewFishNotification")
local RE_SpawnTotem = GetRemote("RE/SpawnTotem")
local RF_EquipOxygenTank = GetRemote("RF/EquipOxygenTank")
local RF_UnequipOxygenTank = GetRemote("RF/UnequipOxygenTank")
local RF_InitiateTrade = GetRemote("RF/InitiateTrade")
local RF_PurchaseMarketItem = GetRemote("RF/PurchaseMarketItem")
local RF_UpdateAutoSellThreshold = GetRemote("RF/UpdateAutoSellThreshold")
local RF_PurchaseWeatherEvent = GetRemote("RF/PurchaseWeatherEvent")

local function GetFishNameAndRarity(item)
    local name = item.Identifier or "Unknown"
    local rarity = item.Metadata and item.Metadata.Rarity or "COMMON"
    local itemID = item.Id
    local itemData
    if ItemUtility and itemID then
        pcall(function()
            itemData = ItemUtility:GetItemData(itemID)
            if not itemData then
                local numericID = tonumber(item.Id) or tonumber(item.Identifier)
                if numericID then itemData = ItemUtility:GetItemData(numericID) end
            end
        end)
    end
    if itemData and itemData.Data and itemData.Data.Name then name = itemData.Data.Name end
    if item.Metadata and item.Metadata.Rarity then 
        rarity = item.Metadata.Rarity 
    elseif itemData and itemData.Probability and TierUtility then 
        pcall(function() rarity = TierUtility:GetTierFromRarity(itemData.Probability.Chance).Name end) 
    end
    return name, rarity
end

local function GetMutation(item)
    if not item.Metadata then return "None" end
    if item.Metadata.VariantId and item.Metadata.VariantId ~= "" then return item.Metadata.VariantId end
    if item.Metadata.Shiny == true then return "Shiny" end
    return "None"
end

local function TeleportToLookAt(position, lookVector)
    local char = lp.Character
    if not char then return end
    local hum = char:FindFirstChild("Humanoid")
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hum and hum.Sit then hum.Sit = false task.wait(0.1) end
    if hrp then 
        hrp.Velocity = Vector3.new(0, 0, 0)
        hrp.CFrame = CFrame.new(position, position + lookVector) * CFrame.new(0, 0.5, 0)
    end
end

-- ======================================================
-- 1.5 GLOBAL POTATO MODE SYSTEM
-- ======================================================
local VFXControllerModule
pcall(function() VFXControllerModule = require(Services.ReplicatedStorage:WaitForChild("Controllers").VFXController) end)

local function DisableAnimations()
    local character = lp.Character
    if not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end
    local animateScript = character:FindFirstChild("Animate")
    if animateScript and animateScript:IsA("LocalScript") and animateScript.Enabled then animateScript.Enabled = false end
    local animator = humanoid:FindFirstChildOfClass("Animator")
    if animator then animator:Destroy() end
end

local function ApplyExtremePotato()
    if VFXControllerModule then
        VFXControllerModule.Handle = function(...) return nil end
        if VFXControllerModule.PlayVFX then VFXControllerModule.PlayVFX.Fire = function(...) return nil end end
        local cosmeticFolder = workspace:FindFirstChild("CosmeticFolder")
        if cosmeticFolder then pcall(function() cosmeticFolder:ClearAllChildren() end) end
    end
    
    pcall(function()
        local CutsceneController = require(Services.ReplicatedStorage:WaitForChild("Controllers"):WaitForChild("CutsceneController"))
        if CutsceneController and CutsceneController.Play then CutsceneController.Play = function() return end end
    end)
    DisableAnimations()
    
    pcall(function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
        settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level04
    end)

    pcall(function()
        Services.Lighting.GlobalShadows = false
        Services.Lighting.FogEnd = 9e9
        Services.Lighting.Brightness = 0
        for _, v in pairs(Services.Lighting:GetChildren()) do
            if v:IsA("PostEffect") or v:IsA("Sky") or v:IsA("Atmosphere") or v:IsA("ColorCorrectionEffect") or v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("DepthOfFieldEffect") then
                v:Destroy()
            end
        end
        if workspace.Terrain then
            workspace.Terrain.WaterWaveSize = 0
            workspace.Terrain.WaterTransparency = 1
            workspace.Terrain.WaterReflectance = 0
            workspace.Terrain.Decoration = false 
        end
    end)

    for _, v in ipairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") then 
            v.Transparency = 1
            v.Reflectance = 0
            v.CastShadow = false
            v.Material = Enum.Material.SmoothPlastic 
        elseif v:IsA("Decal") or v:IsA("Texture") or v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") or v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") or v:IsA("PointLight") or v:IsA("SurfaceLight") or v:IsA("SpotLight") then 
            v:Destroy()
        end
    end

    if lp.Character then
        for _, v in ipairs(lp.Character:GetDescendants()) do
            if v:IsA("BasePart") then 
                v.Transparency = 1
                v.Reflectance = 0
                v.Material = Enum.Material.SmoothPlastic
            elseif v:IsA("Decal") or v:IsA("Texture") or v:IsA("Clothing") or v:IsA("ShirtGraphic") or v:IsA("Accessory") then 
                v:Destroy() 
            end
            if v:IsA("MeshPart") then v.Transparency = 1 end
        end
    end

    local sethidden = sethiddenproperty or set_hidden_property or set_hidden_prop
    if sethidden then pcall(function() sethidden(Services.Lighting, "Technology", Enum.Technology.Compatibility) end) end
    if setfpscap then pcall(function() setfpscap(10) end) end
end

local PotatoLoopThread = nil
local function StartPotatoLoop()
    if PotatoLoopThread then return end
    print("🥔 [Global Potato Mode] Diaktifkan!")
    ApplyExtremePotato()
    PotatoLoopThread = task.spawn(function() while task.wait(5) do ApplyExtremePotato() end end)
end

if _G.EnableGlobalPotatoMode then StartPotatoLoop() end

-- ======================================================
-- 2. LVCB PROGRESSION SYSTEM (LEVER & RUIN)
-- ======================================================
local FishingLocations = {
    ["Arrow Lever"] = {Pos = Vector3.new(898.296, 8.449, -361.856), Look = Vector3.new(0.023, -0.000, 1.000)},
    ["Cresent Lever"] = {Pos = Vector3.new(1419.750, 31.199, 78.570), Look = Vector3.new(0.000, -0.000, -1.000)},
    ["Ancient Ruin"] = {Pos = Vector3.new(6031.981, -585.924, 4713.157), Look = Vector3.new(0.316, -0.000, -0.949)},
    ["Diamond Lever"] = {Pos = Vector3.new(1818.930, 8.449, -284.110), Look = Vector3.new(0.000, 0.000, -1.000)},
    ["Hourglass Diamond Lever"] = {Pos = Vector3.new(1484.610, 8.450, -861.010), Look = Vector3.new(-0.000, -0.000, -1.000)},
    ["Sacred Temple"] = {Pos = Vector3.new(1461.815, -22.125, -670.234), Look = Vector3.new(-0.990, -0.000, 0.143)},
    ["Treasure Room"] = {Pos = Vector3.new(-3561.297, -279.074, -1606.289), Look = Vector3.new(-0.782, -0.000, -0.623)},
}

_G.CurrentPhase = "Loading..."
_G.CurrentAction = "Idle"
_G.CurrentTargetFish = nil 
_G.TotalFishCaught = 0
_G.UnsoldFishCount = 0
_G.LeverProgress = "0/4"
_G.RuinStatusStr = "LOCKED"
_G.IsAutoFishing = true 
_G.PauseFishing = false

local AutoSellConfig = { Enabled = true, Threshold = 100 }

local function CreateDashboardLVCB()
    if Services.CoreGui:FindFirstChild("SynceHubDashboard") then Services.CoreGui.SynceHubDashboard:Destroy() end
    local gui = Instance.new("ScreenGui", Services.CoreGui)
    gui.Name = "SynceHubDashboard"
    gui.IgnoreGuiInset = true

    local dashboard = Instance.new("Frame", gui)
    dashboard.Size = UDim2.fromScale(1, 1)
    dashboard.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    dashboard.BorderSizePixel = 0
    dashboard.Visible = true

    local toggleBtn = Instance.new("TextButton", gui)
    toggleBtn.Size = UDim2.new(0.3, 0, 0.08, 0)
    toggleBtn.Position = UDim2.new(0.5, 0, 0.95, 0)
    toggleBtn.AnchorPoint = Vector2.new(0.5, 1)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
    toggleBtn.Text = "Tutup / Buka GUI"
    toggleBtn.TextColor3 = Color3.new(1,1,1)
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.TextScaled = true
    toggleBtn.ZIndex = 999 
    Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(1,0)
    
    local isGuiOpen = true
    toggleBtn.MouseButton1Click:Connect(function() 
        isGuiOpen = not isGuiOpen
        dashboard.Visible = isGuiOpen 
        toggleBtn.BackgroundTransparency = isGuiOpen and 0 or 0.5
    end)

    local gridContainer = Instance.new("Frame", dashboard)
    gridContainer.Size = UDim2.new(0.95, 0, 0.5, 0)
    gridContainer.Position = UDim2.new(0.5, 0, 0.5, 0)
    gridContainer.AnchorPoint = Vector2.new(0.5, 0.5)
    gridContainer.BackgroundTransparency = 1
    
    local layout = Instance.new("UIListLayout", gridContainer)
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 0)

    local function CreateRow(order)
        local row = Instance.new("Frame", gridContainer)
        row.Size = UDim2.new(1, 0, 0.23, 0) 
        row.BackgroundTransparency = 1
        row.LayoutOrder = order
        return row
    end

    local function CreateCol(parent, text, posX)
        local lab = Instance.new("TextLabel", parent)
        lab.Size = UDim2.new(0.5, 0, 1, 0)
        lab.Position = UDim2.new(posX, 0, 0, 0)
        lab.BackgroundTransparency = 1
        lab.Text = text
        lab.TextColor3 = Color3.new(1,1,1)
        lab.Font = Enum.Font.GothamBold
        lab.TextScaled = true
        return lab
    end

    local function CreateLine()
        local line = Instance.new("Frame", gridContainer)
        line.Size = UDim2.new(1, 0, 0, 2)
        line.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        line.BorderSizePixel = 0
        line.LayoutOrder = 99 
        return line
    end

    local function CreateVLine(parent)
        local line = Instance.new("Frame", parent)
        line.Size = UDim2.new(0, 2, 1, 0)
        line.Position = UDim2.new(0.5, 0, 0, 0)
        line.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        line.BorderSizePixel = 0
        line.ZIndex = 2
    end

    local Row1 = CreateRow(1)
    local L_Phase = CreateCol(Row1, "Fase:\n...", 0)
    L_Phase.TextColor3 = Color3.fromRGB(0, 255, 255)
    CreateVLine(Row1)
    local L_Action = CreateCol(Row1, "Aksi:\n...", 0.5)
    L_Action.TextColor3 = Color3.fromRGB(255, 255, 100)
    local Line1 = CreateLine()
    Line1.LayoutOrder = 2 

    local Row2 = CreateRow(3)
    local L_Levers = CreateCol(Row2, "Lever Terbuka:\n0/4", 0)
    CreateVLine(Row2)
    local L_Ruin = CreateCol(Row2, "Ruin Door:\nLocked", 0.5)
    local Line2 = CreateLine()
    Line2.LayoutOrder = 4 

    local Row3 = CreateRow(5)
    local L_Caught = CreateCol(Row3, "Total Tangkapan:\n0", 0)
    CreateVLine(Row3)
    local L_Unsold = CreateCol(Row3, "Ikan Belum Dijual:\n0", 0.5)
    local Line3 = CreateLine()
    Line3.LayoutOrder = 6 

    local Row4 = CreateRow(7)
    local L_AutoFish = CreateCol(Row4, "Auto Fishing:\nON", 0)
    CreateVLine(Row4)
    local L_Target = CreateCol(Row4, "Smart Target:\nNone", 0.5)
    L_Target.TextColor3 = Color3.fromRGB(255, 150, 255)

    task.spawn(function()
        while task.wait(0.2) do
            if dashboard.Visible then
                L_Phase.Text = "Fase Saat Ini:\n" .. _G.CurrentPhase
                L_Action.Text = "Status Aksi:\n" .. _G.CurrentAction
                
                L_Levers.Text = "Lever Terbuka:\n" .. _G.LeverProgress
                L_Levers.TextColor3 = (_G.LeverProgress == "4/4") and Color3.fromRGB(0, 255, 100) or Color3.new(1,1,1)
                
                L_Ruin.Text = "Ruin Door:\n" .. _G.RuinStatusStr
                L_Ruin.TextColor3 = (_G.RuinStatusStr == "UNLOCKED") and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(255, 50, 50)
                
                L_Caught.Text = "Total Tangkapan:\n" .. tostring(_G.TotalFishCaught)
                L_Unsold.Text = "Ikan Belum Dijual:\n" .. tostring(_G.UnsoldFishCount)
                
                L_AutoFish.Text = "Auto Fishing:\n" .. (_G.IsAutoFishing and (_G.PauseFishing and "PAUSED" or "ACTIVE") or "OFF")
                if _G.PauseFishing then L_AutoFish.TextColor3 = Color3.fromRGB(255, 170, 0)
                elseif _G.IsAutoFishing then L_AutoFish.TextColor3 = Color3.fromRGB(0, 255, 100)
                else L_AutoFish.TextColor3 = Color3.fromRGB(255, 50, 50) end

                L_Target.Text = "Target Utama:\n" .. (_G.CurrentTargetFish or "Tidak Ada (Bebas)")
            end
        end
    end)
end

local ITEM_FISH_NAMES = {"Freshwater Piranha", "Goliath Tiger", "Sacred Guardian Squid", "Crocodile"}
local FishToRarity = {
    ["Freshwater Piranha"] = "Rare", ["Goliath Tiger"] = "Epic", 
    ["Sacred Guardian Squid"] = "Legendary", ["Crocodile"] = "Mythic"
}

local function IsFishPlaced(itemName)
    local ruinFolder = workspace:FindFirstChild("RUIN INTERACTIONS")
    local platesFolder = ruinFolder and ruinFolder:FindFirstChild("PressurePlates")
    if not platesFolder then return false end
    local rarity = FishToRarity[itemName]
    if not rarity then return false end
    local plateModel = platesFolder:FindFirstChild(rarity)
    if plateModel then
        local movePart = plateModel:FindFirstChild("MOVE_PART")
        local stepPlate = movePart and movePart:FindFirstChild("StepPlate")
        if stepPlate and stepPlate.Position.Y < -541.1 then return true end
    end
    return false
end

local function StartBackgroundTasksLVCB()
    task.spawn(function()
        while _G.IsAutoFishing do
            if _G.UnsoldFishCount >= AutoSellConfig.Threshold and RF_SellAllItems then
                local prevAction = _G.CurrentAction
                _G.CurrentAction = "Menjual Ikan (Auto Sell)"
                pcall(function() RF_SellAllItems:InvokeServer() end)
                _G.UnsoldFishCount = 0
                task.wait(1.5)
                _G.CurrentAction = prevAction
            end
            task.wait(1)
        end
    end)
end

local function StartAutoFishingLVCB()
    task.spawn(function()
        while _G.IsAutoFishing do
            local holdingArtifact = false
            if lp.Character then
                local currentTool = lp.Character:FindFirstChildOfClass("Tool")
                if currentTool and (string.find(currentTool.Name, "Artifact") or string.find(currentTool.Name, "Lever")) then holdingArtifact = true end
            end

            if _G.PauseFishing or holdingArtifact then
                task.wait(0.5) 
            else
                if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
                    if not lp.Character:FindFirstChildOfClass("Tool") and Remote_EquipTool then
                        pcall(function() 
                            if Remote_EquipTool:IsA("RemoteFunction") then Remote_EquipTool:InvokeServer(1) else Remote_EquipTool:FireServer(1) end
                        end)
                        task.wait(0.3)
                    end
                    if _G.PauseFishing or holdingArtifact then continue end 
                    if RF_ChargeFishingRod then FireRemote(RF_ChargeFishingRod) end
                    task.wait(0.65)
                    if _G.PauseFishing or holdingArtifact then continue end 
                    if RF_RequestFishingMinigameStarted then FireRemote(RF_RequestFishingMinigameStarted) end
                    task.wait(0.35)
                    if _G.PauseFishing or holdingArtifact then continue end 
                    if RE_FishingCompleted then FireRemote(RE_FishingCompleted) end
                    task.wait(0.3)
                end
            end
            if not lp.Character then task.wait(1) end
        end
    end)
end

local ArtifactData = {
    ["Hourglass Diamond Artifact"] = { FishingPos = {Pos = Vector3.new(1490.144, 3.312, -843.171), Look = Vector3.new(0.115, 0.000, 0.993)}, TargetPos = FishingLocations["Hourglass Diamond Lever"] },
    ["Diamond Artifact"] = { FishingPos = {Pos = Vector3.new(1844.159, 2.530, -288.755), Look = Vector3.new(0.981, 0.000, -0.193)}, TargetPos = FishingLocations["Diamond Lever"] },
    ["Arrow Artifact"] = { FishingPos = {Pos = Vector3.new(874.365, 2.530, -358.484), Look = Vector3.new(-0.990, 0.000, 0.144)}, TargetPos = FishingLocations["Arrow Lever"] },
    ["Crescent Artifact"] = { FishingPos = {Pos = Vector3.new(1401.070, 6.489, 116.738), Look = Vector3.new(-0.500, -0.000, 0.866)}, TargetPos = FishingLocations["Cresent Lever"] },
}
local ArtifactOrder = {"Hourglass Diamond Artifact", "Diamond Artifact", "Arrow Artifact", "Crescent Artifact"}
local ARTIFACT_IDS = { ["Arrow Artifact"] = 265, ["Crescent Artifact"] = 266, ["Diamond Artifact"] = 267, ["Hourglass Diamond Artifact"] = 271 }

local function GetUnlockedArtifacts()
    local JUNGLE = workspace:FindFirstChild("JUNGLE INTERACTIONS")
    local unlocked = {}
    local count = 0
    if not JUNGLE then return unlocked, count end

    for _, desc in ipairs(JUNGLE:GetDescendants()) do
        if desc.Name == "Crystal" and desc:IsA("BasePart") then
            local r, g, b = desc.Color.R * 255, desc.Color.G * 255, desc.Color.B * 255
            
            if math.abs(r - 219) <= 4 and math.abs(g - 38) <= 4 and math.abs(b - 255) <= 4 then 
                unlocked["Hourglass Diamond Artifact"] = true; count = count + 1
            elseif math.abs(r - 255) <= 4 and math.abs(g - 248) <= 4 and math.abs(b - 49) <= 4 then 
                unlocked["Diamond Artifact"] = true; count = count + 1
            elseif math.abs(r - 255) <= 4 and math.abs(g - 47) <= 4 and math.abs(b - 47) <= 4 then 
                unlocked["Arrow Artifact"] = true; count = count + 1
            elseif math.abs(r - 112) <= 4 and math.abs(g - 255) <= 4 and math.abs(b - 69) <= 4 then 
                unlocked["Crescent Artifact"] = true; count = count + 1
            end
        end
    end
    return unlocked, count
end

local function HasArtifactItem(artifactName)
    if lp.Character then
        for _, v in ipairs(lp.Character:GetChildren()) do
            if v:IsA("Tool") and string.find(v.Name, artifactName) then return true end
        end
    end
    local backpack = lp:FindFirstChild("Backpack")
    if backpack then
        for _, v in ipairs(backpack:GetChildren()) do
            if v:IsA("Tool") and string.find(v.Name, artifactName) then return true end
        end
    end
    if not PlayerData then return false end
    local hasItem = false
    pcall(function()
        local inv = PlayerData:Get("Inventory")
        local targetId = ARTIFACT_IDS[artifactName]
        for _, items in pairs(inv) do
            for _, item in ipairs(items) do
                if tonumber(item.Id) == targetId then hasItem = true return end
            end
        end
    end)
    return hasItem
end

local placementAttempts = {} 

local function DoAutoLever()
    _G.CurrentPhase = "Auto Lever (1/2)"
    local globalRechecks = 0

    while _G.IsAutoFishing do
        local unlockedArtifacts, unlockedCount = GetUnlockedArtifacts()
        _G.LeverProgress = tostring(unlockedCount) .. "/4"
        
        local allDoneOrSkipped = true
        local artifactToProcess = nil
        
        for _, artifactName in ipairs(ArtifactOrder) do
            if not unlockedArtifacts[artifactName] then
                if (placementAttempts[artifactName] or 0) < 3 then
                    allDoneOrSkipped = false
                    if not artifactToProcess then artifactToProcess = artifactName end
                end
            end
        end

        if allDoneOrSkipped then
            _G.CurrentAction = "Memverifikasi Pemasangan Lever (Re-check)..."
            task.wait(3) -- Beri waktu delay agar server visual sync
            
            local recheckArtifacts, recheckCount = GetUnlockedArtifacts()
            if recheckCount >= 4 then
                _G.CurrentAction = "Semua Lever Selesai & Valid!"
                break
            else
                if globalRechecks < 3 then
                    _G.CurrentAction = "Terdeteksi Miss! Mengulang Pemasangan..."
                    globalRechecks = globalRechecks + 1
                    -- Reset hitungan untuk lever yang miss agar script mengulang
                    for _, artifactName in ipairs(ArtifactOrder) do
                        if not recheckArtifacts[artifactName] then
                            placementAttempts[artifactName] = 0 
                        end
                    end
                    task.wait(2)
                else
                    _G.CurrentAction = "Lever Selesai (Limit Recheck Maksimal!)"
                    break
                end
            end
        elseif artifactToProcess then
            local artData = ArtifactData[artifactToProcess]
            local char = lp.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChild("Humanoid")

            if HasArtifactItem(artifactToProcess) then
                local currentAttempts = (placementAttempts[artifactToProcess] or 0) + 1
                _G.CurrentAction = "Memasang: " .. artifactToProcess .. " (" .. currentAttempts .. "/3)"
                _G.PauseFishing = true 
                task.wait(0.5)
                
                if hrp and (hrp.Position - artData.TargetPos.Pos).Magnitude > 5 then
                    TeleportToLookAt(artData.TargetPos.Pos, artData.TargetPos.Look)
                    task.wait(1.5) 
                end

                if hrp then hrp.Anchored = true end
                
                local backpack = lp:FindFirstChild("Backpack")
                if hum and backpack then
                    for _, tool in ipairs(backpack:GetChildren()) do
                        if tool:IsA("Tool") and string.find(tool.Name, artifactToProcess) then
                            hum:EquipTool(tool)
                            break
                        end
                    end
                end
                
                task.wait(1.5) 
                if RE_PlaceLeverItem then 
                    FireRemote(RE_PlaceLeverItem, artifactToProcess) 
                    placementAttempts[artifactToProcess] = currentAttempts 
                end
                task.wait(2.5) 
                if hrp then hrp.Anchored = false end
            else
                _G.CurrentAction = "Mencari: " .. artifactToProcess
                _G.PauseFishing = false 
                if hrp and (hrp.Position - artData.FishingPos.Pos).Magnitude > 5 then
                    TeleportToLookAt(artData.FishingPos.Pos, artData.FishingPos.Look)
                    task.wait(1.5)
                end
                task.wait(1) 
            end
        end
        task.wait(0.1)
    end
end

local function GetRuinDoorStatus()
    local ruinDoor = workspace:FindFirstChild("RUIN INTERACTIONS") and workspace["RUIN INTERACTIONS"]:FindFirstChild("Door")
    local status = "LOCKED"
    if ruinDoor and ruinDoor:FindFirstChild("RuinDoor") then
        local LDoor = ruinDoor.RuinDoor:FindFirstChild("LDoor")
        if LDoor then
            local currentX = nil
            if LDoor:IsA("BasePart") then currentX = LDoor.Position.X
            elseif LDoor:IsA("Model") then local s, p = pcall(function() return LDoor:GetPivot() end) if s and p then currentX = p.Position.X end end
            if currentX ~= nil and currentX > 6075 then status = "UNLOCKED" end
        end
    end
    _G.RuinStatusStr = status
    return status
end

local function IsItemAvailable(itemName)
    if not PlayerData then return false end
    local hasItem = false
    pcall(function()
        local inv = PlayerData:Get("Inventory")
        for _, items in pairs(inv) do
            for _, item in ipairs(items) do
                local name = GetFishNameAndRarity(item)
                if name == itemName then hasItem = true return end
            end
        end
    end)
    return hasItem
end

local function GetMissingItem()
    for _, name in ipairs(ITEM_FISH_NAMES) do
        if not IsFishPlaced(name) and not IsItemAvailable(name) then return name end
    end
    return nil
end

local function DoAutoRuin()
    _G.CurrentPhase = "Auto Ruin Door (2/2)"
    local runningRuin = true
    
    task.spawn(function()
        while runningRuin and _G.IsAutoFishing do
            if GetRuinDoorStatus() == "LOCKED" and RE_PlacePressureItem then
                for _, name in ipairs(ITEM_FISH_NAMES) do
                    if IsItemAvailable(name) and not IsFishPlaced(name) then
                        _G.CurrentAction = "Memasang: " .. name
                        FireRemote(RE_PlacePressureItem, name)
                        task.wait(2.1)
                    end
                end
            end
            task.wait(3)
        end
    end)

    while _G.IsAutoFishing do
        local doorStatus = GetRuinDoorStatus()
        if doorStatus == "UNLOCKED" then
            _G.CurrentAction = "Ruin Door Terbuka!"
            runningRuin = false
            break 
        end

        local missingItem = GetMissingItem()
        _G.CurrentTargetFish = missingItem 

        local char = lp.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")

        if missingItem then
            _G.CurrentAction = "Farming: " .. missingItem
            _G.PauseFishing = false 
            if hrp and (hrp.Position - FishingLocations["Sacred Temple"].Pos).Magnitude > 5 then
                TeleportToLookAt(FishingLocations["Sacred Temple"].Pos, FishingLocations["Sacred Temple"].Look)
                task.wait(1.5)
            end
            task.wait(1) 
        else
            _G.CurrentAction = "Menunggu Pintu Ruin..."
            _G.PauseFishing = true 
            task.wait(0.5)
            if hrp and (hrp.Position - FishingLocations["Ancient Ruin"].Pos).Magnitude > 50 then
                 TeleportToLookAt(FishingLocations["Ancient Ruin"].Pos, FishingLocations["Ancient Ruin"].Look)
                 task.wait(1.5)
            end
            task.wait(1)
        end
    end
end

-- ======================================================
-- 3. REMFARM ENDGAME SYSTEM (FARMING & TRADING)
-- ======================================================
local OnlineConfigUrl = "http://43.134.116.2:5000/config.json"
local MasterConfig = {
    FishingEnabled = true, AutoEquip = true, DelayChargeToReq = 0.65, DelayReqToCatch = 0.35,
    AutoSell = true, SellDelay = 65, AutoFavorite = true, AutoUnfavoriteRubies = false, 
    AutoTeleport = true, SelectedLocation = "Ancient Ruin", AutoTotem = true,
    AutoBuyTotem = false, AutoBuyWeather = false, TargetWeathers = {"Cloudy", "Storm", "Wind"}, WeatherLoopDelay = 300, 
    PotatoMode = true, AutoTrade = false, TargetUsername = "mm3kenak", TradeMode = "SECRETS_FIRST", TradeDelay = 6.5,
    WebhookEnabled = false, WebhookUrl = "http://43.134.116.2:5000/webhook/fishit",
}

local FishingState = { IsFishing = false, CurrentMode = "INSTANT_RECHARGE", Teleporting = false, TotalCoins = 0, SessionEarnings = 0 }
local TradingState = { IsTrading = false, Thread = nil }
local FishingThreads = {}
local TotalFishCaught = 0

local function GetCoins() return PlayerData and (PlayerData:Get("Coins") or 0) or 0 end

local function DisableNativeAutoFish()
    pcall(function() 
        local AutoFishCtrl = require(Services.ReplicatedStorage.Controllers.AutoFishingController) 
        if AutoFishCtrl.Stop then AutoFishCtrl:Stop() end
        if AutoFishCtrl.Disable then AutoFishCtrl:Disable() end
        AutoFishCtrl.Start = function() return end 
    end)
end

local function StartFishingRouterRemfarm()
    if FishingState.IsFishing then return end
    FishingState.IsFishing = true
    DisableNativeAutoFish()

    table.insert(FishingThreads, task.spawn(function()
        while FishingState.IsFishing do
            if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
                if MasterConfig.AutoEquip and not lp.Character:FindFirstChildOfClass("Tool") then
                    if Remote_EquipTool then pcall(function() Remote_EquipTool:InvokeServer(1) end) end
                    task.wait(0.3)
                end
                if RF_ChargeFishingRod then FireRemote(RF_ChargeFishingRod) end
                task.wait(MasterConfig.DelayChargeToReq)
                if not FishingState.IsFishing then break end
                if RF_RequestFishingMinigameStarted then FireRemote(RF_RequestFishingMinigameStarted) end
                task.wait(MasterConfig.DelayReqToCatch)
                if not FishingState.IsFishing then break end
                if RE_FishingCompleted then FireRemote(RE_FishingCompleted) end
            end
            if not lp.Character then task.wait(1) end
        end
    end))

    table.insert(FishingThreads, task.spawn(function()
        while FishingState.IsFishing do
            if MasterConfig.AutoSell then
                if RF_SellAllItems then pcall(function() RF_SellAllItems:InvokeServer() end) end
            end
            task.wait(MasterConfig.SellDelay)
        end
    end))

    table.insert(FishingThreads, task.spawn(function()
        while FishingState.IsFishing do
            if MasterConfig.AutoFavorite then
                local inv = PlayerData:GetExpect("Inventory")
                if inv then
                    for _, items in pairs(inv) do
                        for _, item in ipairs(items) do
                            if item.UUID then
                                local itemName = GetFishNameAndRarity(item)
                                local mutation = GetMutation(item)
                                local isFavorited = (item.IsFavorite or item.Favorited or item.Locked)
                                if (itemName == "Ruby" and mutation == "Gemstone") and not isFavorited then
                                    task.spawn(function() if RE_FavoriteItem then RE_FavoriteItem:FireServer(item.UUID) end end)
                                    task.wait(0.5)
                                end
                            end
                        end
                    end
                end
            end
            task.wait(3)
        end
    end))
end

local function LoadWebConfig()
    if not OnlineConfigUrl or OnlineConfigUrl == "" or OnlineConfigUrl:find("example.com") then return end
    local success, response = pcall(function() return game:HttpGet(OnlineConfigUrl, true) end)
    if success then
        local decodeSuccess, decodedData = pcall(function() return Services.HttpService:JSONDecode(response) end)
        if decodeSuccess then
            for key, value in pairs(decodedData) do 
                if key ~= "SelectedLocation" then MasterConfig[key] = value end 
            end
        end
    end
end

local function CreateDashboardRemfarm()
    if Services.CoreGui:FindFirstChild("EnochDashboard") then Services.CoreGui.EnochDashboard:Destroy() end
    local gui = Instance.new("ScreenGui", Services.CoreGui)
    gui.Name = "EnochDashboard"
    gui.IgnoreGuiInset = true

    local dashboard = Instance.new("Frame", gui)
    dashboard.Size = UDim2.fromScale(1, 1)
    dashboard.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    dashboard.Visible = true

    local toggleBtn = Instance.new("TextButton", gui)
    toggleBtn.Size = UDim2.new(0.3, 0, 0.08, 0)
    toggleBtn.Position = UDim2.new(0.5, 0, 0.95, 0)
    toggleBtn.AnchorPoint = Vector2.new(0.5, 1)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
    toggleBtn.Text = "Tutup / Buka GUI (Remfarm)"
    toggleBtn.TextColor3 = Color3.new(1,1,1)
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.TextScaled = true
    toggleBtn.ZIndex = 999 
    Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(1,0)
    
    toggleBtn.MouseButton1Click:Connect(function() 
        dashboard.Visible = not dashboard.Visible 
        toggleBtn.BackgroundTransparency = dashboard.Visible and 0 or 0.5
    end)

    local L_Status = Instance.new("TextLabel", dashboard)
    L_Status.Size = UDim2.new(1, 0, 0.2, 0)
    L_Status.Position = UDim2.new(0, 0, 0.4, 0)
    L_Status.BackgroundTransparency = 1
    L_Status.TextColor3 = Color3.fromRGB(0, 255, 100)
    L_Status.Font = Enum.Font.GothamBold
    L_Status.TextScaled = true

    task.spawn(function()
        while dashboard.Parent and task.wait(0.5) do
            L_Status.Text = "Status: Remfarm Endgame Active\nCaught: " .. TotalFishCaught
        end
    end)
end

local function StartMerchantAutoBuy()
    task.spawn(function()
        while true do
            if MasterConfig.AutoBuyTotem then
                local currentCoins = GetCoins()
                if currentCoins > 1000000 and RF_PurchaseMarketItem then
                    pcall(function() RF_PurchaseMarketItem:InvokeServer(8) end)
                end
            end
            task.wait(3) 
        end
    end)
end

local function RunAutoTrade()
    if TradingState.IsTrading then return end
    TradingState.IsTrading = true
    TradingState.Thread = task.spawn(function()
        while MasterConfig.AutoTrade and TradingState.IsTrading do
            local targetPlayer = Services.Players:FindFirstChild(MasterConfig.TargetUsername)
            if targetPlayer and RF_InitiateTrade then
                local inv = PlayerData:GetExpect("Inventory")
                if inv then
                    local itemTraded = false
                    for _, items in pairs(inv) do
                        if itemTraded then break end
                        for _, item in ipairs(items) do
                            local name, rarity = GetFishNameAndRarity(item)
                            
                            local isSecret = (tostring(rarity):upper() == "SECRET")
                            local isRubyGem = (name == "Ruby" and GetMutation(item) == "Gemstone")
                            local isEvolvedStone = (tostring(item.Id) == "558" or string.find(string.lower(name), "evolved enchant stone"))

                            if isSecret or isRubyGem or isEvolvedStone then
                                pcall(function() RF_InitiateTrade:InvokeServer(targetPlayer.UserId, item.UUID) end)
                                itemTraded = true
                                task.wait(5)
                                break
                            end
                        end
                    end
                end
            end
            task.wait(MasterConfig.TradeDelay)
        end
        TradingState.IsTrading = false
    end)
end

if RE_ObtainedNewFishNotification then
    RE_ObtainedNewFishNotification.OnClientEvent:Connect(function(item)
        _G.TotalFishCaught = (_G.TotalFishCaught or 0) + 1
        _G.UnsoldFishCount = (_G.UnsoldFishCount or 0) + 1
        TotalFishCaught = (TotalFishCaught or 0) + 1
    end)
end

-- ======================================================
-- 4. THE BRIDGE: LVCB -> REMFARM CONTROLLER
-- ======================================================
task.spawn(function()
    if GetRuinDoorStatus() ~= "UNLOCKED" then
        print("🚪 Pintu Ruin masih terkunci. Menjalankan LVCB Script...")
        
        CreateDashboardLVCB() 
        StartBackgroundTasksLVCB() 
        StartAutoFishingLVCB()
        
        DoAutoLever() 
        DoAutoRuin()  
        
        print("✅ Ruin Terbuka! Mematikan LVCB...")
        _G.IsAutoFishing = false
        _G.PauseFishing = true
        
        if Services.CoreGui:FindFirstChild("SynceHubDashboard") then 
            Services.CoreGui.SynceHubDashboard:Destroy() 
        end
        task.wait(2) 
    else
        print("✅ Pintu Ruin sudah terbuka dari awal.")
    end

    -- TRANSISI KE REMFARM
    print("🚀 Memulai Remfarm V8.6 Full Auto...")
    
    MasterConfig.SelectedLocation = "Ancient Ruin"
    
    CreateDashboardRemfarm()
    LoadWebConfig()
    
    if RF_UpdateAutoSellThreshold then 
        pcall(function() RF_UpdateAutoSellThreshold:InvokeServer(6) end) 
    end

    StartMerchantAutoBuy()
    
    TeleportToLookAt(FishingLocations["Ancient Ruin"].Pos, FishingLocations["Ancient Ruin"].Look)
    task.wait(1)
    
    if MasterConfig.FishingEnabled then StartFishingRouterRemfarm() end
    if MasterConfig.AutoTrade then RunAutoTrade() end
    
    print("🎉 Transisi Selesai. Farming abadi di Ruin berjalan sepenuhnya via Remfarm!")
end)
