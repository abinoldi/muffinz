#!/system/bin/sh

# ==========================================================
# ROBLOX INJECTOR v7.2 (DELETE OLD DB & REPLACE)
# ==========================================================

TERMUX_SQLITE="/data/data/com.termux/files/usr/bin/sqlite3"
TMP_SQLITE="/data/local/tmp/sqlite3_exec"
TEMP_DB_DIR="/data/local/tmp/rbx_temp_cookies"
COOKIE_QUEUE="/data/local/tmp/rbx_queue.txt"

check_root() {
    if [ "$(id -u)" -ne 0 ]; then
        echo "❌ Script ini MEMBUTUHKAN akses Root!"
        exit 1
    fi
}

prepare_env() {
    # Siapkan Sqlite
    rm -f "$TMP_SQLITE"
    if [ -f "$TERMUX_SQLITE" ]; then
        cp "$TERMUX_SQLITE" "$TMP_SQLITE"
        chmod 755 "$TMP_SQLITE"
        SQLITE_BIN="$TMP_SQLITE"
    else
        SQLITE_BIN=$(which sqlite3)
    fi

    if [ -z "$SQLITE_BIN" ] || [ ! -x "$SQLITE_BIN" ]; then
        echo "❌ sqlite3 tidak ditemukan. Wajib install di Termux."
        exit 1
    fi

    # Siapkan folder temp
    rm -rf "$TEMP_DB_DIR"
    mkdir -p "$TEMP_DB_DIR"
    chmod 777 "$TEMP_DB_DIR"
}

# --- PHASE 1: GENERATE DB (Membuat file matang di luar) ---
generate_db_file() {
    local index="$1"
    local raw_cookie="$2"
    local db_path="$TEMP_DB_DIR/cookie_$index.db"

    # Hapus file temp lama jika ada
    rm -f "$db_path"

    # Create Table (Struktur Lengkap)
    "$SQLITE_BIN" "$db_path" "CREATE TABLE IF NOT EXISTS cookies (creation_utc INTEGER NOT NULL, host_key TEXT NOT NULL, name TEXT NOT NULL, value TEXT NOT NULL, path TEXT NOT NULL, expires_utc INTEGER NOT NULL, is_secure INTEGER NOT NULL, is_httponly INTEGER NOT NULL, last_access_utc INTEGER NOT NULL, has_expires INTEGER NOT NULL DEFAULT 1, is_persistent INTEGER NOT NULL DEFAULT 1, priority INTEGER NOT NULL DEFAULT 1, encrypted_value BLOB DEFAULT '', samesite INTEGER NOT NULL DEFAULT -1, source_scheme INTEGER NOT NULL DEFAULT 0, top_frame_site_key TEXT NOT NULL DEFAULT '', UNIQUE (host_key, name, path));"

    # Insert Cookie
    "$SQLITE_BIN" "$db_path" "INSERT INTO cookies (creation_utc, host_key, name, value, path, expires_utc, is_secure, is_httponly, last_access_utc, has_expires, is_persistent, priority, samesite, source_scheme, top_frame_site_key) VALUES ((strftime('%s','now') * 1000000), '.roblox.com', '.ROBLOSECURITY', '$raw_cookie', '/', 253402300799000000, 1, 1, (strftime('%s','now') * 1000000), 1, 1, 1, -1, 0, '');"

    if [ -f "$db_path" ]; then
        echo "   ✅ Generated DB #$index OK"
    fi
}

# --- PHASE 2: DELETE OLD & REPLACE (Inti Perbaikan) ---
replace_database() {
    local pkg_name="$1"
    local source_db="$2"
    
    local target_root="/data/data/$pkg_name"
    local target_folder="$target_root/app_webview/Default"
    local target_file="$target_folder/Cookies"

    echo "------------------------------------------"
    echo "🔄 Memproses: $pkg_name"

    # 1. Force Stop
    am force-stop "$pkg_name" > /dev/null 2>&1

    # 2. HAPUS DATABASE LAMA (Sesuai Permintaan)
    if [ -e "$target_file" ]; then
        echo "   🗑️  File lama ditemukan. Menghapus..."
        rm -f "$target_file"
    fi

    # 3. PASTIKAN FOLDER INDUK ADA
    if [ ! -d "$target_folder" ]; then
        echo "   📂 Folder belum ada. Membuat..."
        mkdir -p "$target_folder"
        
        # Fix owner folder parent SEBELUM copy
        local app_uid=$(stat -c "%u:%g" "$target_root")
        chown -R "$app_uid" "$target_root/app_webview"
        chmod 755 "$target_root/app_webview"
    fi

    # 4. COPY DATABASE BARU
    echo "   📥 Menyalin database baru..."
    cp "$source_db" "$target_file"

    if [ $? -eq 0 ]; then
        # 5. FIX PERMISSION
        local app_uid=$(stat -c "%u:%g" "$target_root")
        chown -R "$app_uid" "$target_root/app_webview"
        chmod 660 "$target_file"
        echo "   ✨ SUKSES TERPASANG"
    else
        echo "   ❌ Gagal menyalin file."
    fi
}

do_main_process() {
    echo "=========================================="
    echo "   ROBLOX INJECTOR v7.2 (DELETE & COPY)   "
    echo "=========================================="
    prepare_env
    
    # Deteksi App
    pkgs=($(pm list packages | grep "com.roblox.clien" | cut -f 2 -d ":"))
    total_apps=${#pkgs[@]}
    
    if [ $total_apps -eq 0 ]; then
        echo "❌ Tidak ada aplikasi Roblox Clone."
        return
    fi

    # Input Cookie
    echo "📱 Terdeteksi $total_apps aplikasi."
    echo "📝 INPUT COOKIE (Ketik 'done' selesai)"
    echo "------------------------------------------"
    
    rm -f "$COOKIE_QUEUE"
    i=0
    while true; do
        display_num=$((i + 1))
        echo -n "[$display_num] Cookie: "
        read -r input_cookie

        if [ "$input_cookie" = "done" ]; then break; fi
        if [ -n "$input_cookie" ]; then
            echo "$input_cookie" >> "$COOKIE_QUEUE"
            i=$((i + 1))
        fi
        if [ $i -ge $total_apps ]; then echo "⚠️  Slot penuh."; break; fi
    done
    total_cookies=$i

    # Phase 1: Generate di Temp
    echo "------------------------------------------"
    echo "🔨 [1/2] Membuat Database Sementara..."
    
    line_num=1
    while [ $line_num -le $total_cookies ]; do
        current_cookie=$(sed "${line_num}q;d" "$COOKIE_QUEUE")
        generate_db_file "$line_num" "$current_cookie"
        line_num=$((line_num + 1))
    done

    # Phase 2: Delete & Replace
    echo "------------------------------------------"
    echo "🚀 [2/2] Mengganti Database Aplikasi..."
    
    idx=0
    for pkg in "${pkgs[@]}"; do
        db_num=$((idx + 1))
        source_db="$TEMP_DB_DIR/cookie_$db_num.db"

        if [ ! -f "$source_db" ]; then break; fi

        replace_database "$pkg" "$source_db"
        
        idx=$((idx + 1))
    done

    # Bersihkan
    rm -rf "$TEMP_DB_DIR"
    rm -f "$COOKIE_QUEUE"
    echo "------------------------------------------"
    echo "✨ SELESAI! Silakan buka aplikasinya."
}

check_root
do_main_process
