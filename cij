#!/system/bin/sh

# ==========================================================
# ROBLOX MASS INJECTOR v4.2 (BASE ON ORIGINAL v3.9)
# ==========================================================

TERMUX_SQLITE="/data/data/com.termux/files/usr/bin/sqlite3"
TMP_SQLITE="/data/local/tmp/sqlite3_inject"
COOKIE_QUEUE="/data/local/tmp/rbx_cookie_queue.txt"

check_root() {
    if [ "$(id -u)" -ne 0 ]; then
        echo "❌ Script ini MEMBUTUHKAN akses Root!"
        exit 1
    fi
}

prepare_sqlite() {
    rm -f "$TMP_SQLITE"
    if [ -f "$TERMUX_SQLITE" ]; then
        cp "$TERMUX_SQLITE" "$TMP_SQLITE"
        chmod 755 "$TMP_SQLITE"
        SQLITE_BIN="$TMP_SQLITE"
    else
        SQLITE_BIN=$(which sqlite3)
    fi

    if [ -z "$SQLITE_BIN" ] || [ ! -x "$SQLITE_BIN" ]; then
        echo "❌ sqlite3 tidak ditemukan. Pastikan pkg install sqlite di Termux."
        exit 1
    fi
}

# --- FUNGSI INTI (DICOPY DARI v3.9 ORIGINAL) ---
inject_logic() {
    local TARGET_PKG="$1"
    local TARGET_COOKIE="$2"
    local folder="/data/data/$TARGET_PKG"

    # 4. Tentukan Path (Sama seperti v3.9)
    local cookie_path=""
    [ -f "$folder/app_webview/Default/Cookies" ] && cookie_path="$folder/app_webview/Default/Cookies"
    [ -f "$folder/app_webview/Cookies" ] && [ -z "$cookie_path" ] && cookie_path="$folder/app_webview/Cookies"
    
    if [ -z "$cookie_path" ]; then
        mkdir -p "$folder/app_webview/Default"
        cookie_path="$folder/app_webview/Default/Cookies"
    fi

    # 5. Force Stop (Sama seperti v3.9)
    am force-stop "$TARGET_PKG"

    # 6. Injeksi SQL (SAMA PERSIS DENGAN v3.9)
    # Menggunakan top_frame_site_key dan strftime
    "$SQLITE_BIN" "$cookie_path" "DELETE FROM cookies WHERE name='.ROBLOSECURITY';"

    "$SQLITE_BIN" "$cookie_path" "INSERT OR REPLACE INTO cookies (creation_utc, host_key, name, value, path, expires_utc, is_secure, is_httponly, last_access_utc, has_expires, is_persistent, priority, samesite, source_scheme, top_frame_site_key) VALUES ((strftime('%s','now') * 1000000), '.roblox.com', '.ROBLOSECURITY', '$TARGET_COOKIE', '/', 253402300799000000, 1, 1, (strftime('%s','now') * 1000000), 1, 1, 1, -1, 0, '');"

    if [ $? -eq 0 ]; then
        # 7. Fix Permissions (Sama seperti v3.9)
        local app_uid=$(stat -c "%u:%g" "$folder")
        chown -R "$app_uid" "$folder/app_webview"
        chmod 660 "$cookie_path"
        echo "✅ Sukses -> $TARGET_PKG"
    else
        echo "❌ Gagal SQL -> $TARGET_PKG"
    fi
}

do_mass_inject() {
    echo "=========================================="
    echo "    ROBLOX MASS INJECTOR (MULTI-INPUT)    "
    echo "=========================================="
    prepare_sqlite
    
    # Bersihkan antrian lama
    rm -f "$COOKIE_QUEUE"
    touch "$COOKIE_QUEUE"

    # 1. Deteksi Aplikasi
    pkgs=($(pm list packages | grep "com.roblox.clien" | cut -f 2 -d ":"))
    total_apps=${#pkgs[@]}
    
    if [ $total_apps -eq 0 ]; then
        echo "❌ Tidak ada aplikasi Roblox Clone."
        return
    fi

    echo "📱 Terdeteksi $total_apps aplikasi."
    echo "------------------------------------------"
    echo "📝 Masukkan Cookie satu per satu."
    echo "   Ketik 'done' lalu Enter jika sudah selesai."
    echo "------------------------------------------"

    # Loop Input
    i=0
    while true; do
        display_num=$((i + 1))
        echo -n "[$display_num] Cookie: "
        read -r input_cookie

        # Jika user ketik done, berhenti input
        if [ "$input_cookie" = "done" ]; then
            break
        fi

        # Jika input tidak kosong, simpan ke file
        if [ -n "$input_cookie" ]; then
            echo "$input_cookie" >> "$COOKIE_QUEUE"
            i=$((i + 1))
        fi
        
        # Jika jumlah cookie sudah sama dengan jumlah aplikasi, otomatis lanjut
        if [ $i -ge $total_apps ]; then
            echo "⚠️  Slot penuh ($total_apps app). Memulai injeksi..."
            break
        fi
    done

    echo "------------------------------------------"
    echo "🚀 Memproses injeksi..."
    echo "------------------------------------------"

    # Loop Eksekusi (Mencocokkan baris cookie dengan aplikasi)
    line_num=1
    for pkg in "${pkgs[@]}"; do
        # Ambil cookie dari file baris ke-N
        # (Menggunakan sed agar aman dari karakter aneh)
        current_cookie=$(sed "${line_num}q;d" "$COOKIE_QUEUE")
        
        # Jika cookie habis, berhenti
        if [ -z "$current_cookie" ]; then
            break
        fi

        # Jalankan fungsi inject_logic (Engine v3.9)
        inject_logic "$pkg" "$current_cookie"
        
        line_num=$((line_num + 1))
    done

    # Hapus file sementara
    rm -f "$COOKIE_QUEUE"
    echo "------------------------------------------"
    echo "✨ SELESAI!"
}

check_root
do_mass_inject
